@page
@model Asigment1.Pages.Categories.IndexModel
@{
    Layout = "~/Pages/Shared/_Layout.cshtml"; 
}

<h1 class="mt-4 mb-4">Categories</h1>

<table class="table table-striped table-bordered">
    <thead class="thead-dark">
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var category in Model.Categories)
        {
            <tr>
                <td>@category.Id</td>
                <td>@category.Name</td>
                <td>@category.Price.ToString("C2")</td> 
            </tr>
        }
    </tbody>
</table>





@* @page
@model Asigment1.Pages.Categories.IndexModel
@{
    Layout = "~/Pages/Shared/_Layout.cshtml"; // Đường dẫn đến layout
}

<h1 class="mt-4 mb-4">Categories</h1>

<table class="table table-striped table-bordered">
    <thead class="thead-dark">
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var category in Model.Categories)
        {
            <tr>
                <td>@category.Id</td>
                <td>@category.Name</td>
                <td>@category.Price.ToString("C2")</td> <!-- Định dạng giá -->
            </tr>
        }
    </tbody>
</table> *@
